{
  "name": "battle-royale-web",
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc && vite build",
    "preview": "vite preview",
    "deploy": "npm run build && gh-pages -d dist"
  },
  "dependencies": {
    "three": "^0.165.0"
  },
  "devDependencies": {
    "@types/node": "^26.1.1",
    "@types/three": "^0.165.0",
    "gh-pages": "^6.1.0",
    "typescript": "^5.4.0",
    "vite": "^5.2.0"
  }
}
